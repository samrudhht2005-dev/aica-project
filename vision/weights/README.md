# AICA product detector weights
#
# Place trained weights here:
#   aica_product_detector.pt
#
# This folder is intentionally empty until you collect real images and run:
#   python training/scripts/train_product_detector.py
#
# Do NOT copy the legacy synthetic vision/best.pt here — that model was trained
# for 1 epoch on coloured rectangles with printed class names, not real products.
